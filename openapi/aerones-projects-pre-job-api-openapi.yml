openapi: 3.2.0
info:
  title: Operations Hub Projects.pre Job API
  version: 0.1.1
  description: ''
servers: []
tags:
- name: projects.pre-job
paths:
  /api/projects/{project_id}/pre-job/pdf:
    get:
      operationId: projects_api_prejob_get_project_pre_job_pdf
      summary: Get Project Pre Job Pdf
      parameters:
      - in: path
        name: project_id
        schema:
          title: Project Id
          type: string
        required: true
      responses:
        '200':
          description: OK
      description: 'Generate and download pre-job PDF report.


        Args:

        request: The HTTP request

        project_id: The project ID


        Returns:

        HttpResponse: PDF file download response'
      tags:
      - projects.pre-job
      security:
      - APIKeyAuth: []
      - CookieAuth: []
components:
  securitySchemes:
    APIKeyAuth:
      type: http
      scheme: bearer
    CookieAuth:
      type: apiKey
      in: cookie
      name: opshub_prod_sessionid
    AuthBearer:
      type: http
      scheme: bearer
